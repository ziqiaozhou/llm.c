[package]
name = "benchmark"
version = "0.1.0"
edition = "2024"


[[bench]]
name = "matmul"
harness = false

[[bench]]
name = "softmax"
harness = false

[[bench]]
name = "layernorm"
harness = false

[dependencies]
llmc = {path = "../llmc"}
llmrs = {path = "../llmrs"}
gpu_host = { workspace=true }
gpu = {workspace = true}
criterion = { version = "0.5", features = ["html_reports"] }
rand = "0.9"
serde_json = "1.0"
serde = "1.0"